#!{{ lookPath "sh" }}

sudo cp $HOME/.local/share/chezmoi/lib/systemd/system/* /lib/systemd/system/
# here is fine to also enable this script, but need to ensure downloading the right xremap version first
